apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-build-test-helm.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-build-test-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-build-test-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
